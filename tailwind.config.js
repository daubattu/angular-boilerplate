/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./src/**/*.{html,ts}" // Quét tất cả HTML & TS trong src để loại bỏ CSS không dùng
  ],
  theme: {
    extend: {},
  },
  plugins: [],
};